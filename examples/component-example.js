// Create needed html apply bindings if in browser
// The file gets loaded and returns always HTML as Object or custom Element
// This HTML then can be modifyed or binded
// The HTML can be DATA Driven so it changes
// Changing html only needs to be applyed to dom selector
